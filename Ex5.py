def Sumnumber(tab): 
    tab_length = len(tab)

    for i in tab_length: 
        sum_of_number= 0 
        sum_of_number += i 
    print(sum_of_number)


#bonus 

tab_length = len(tab)
def Sumnumber(tab, tab_length)
    if tab_length = 0: 
        return sum_of_number
    else: 
        return  tab[tab_length] + sumnumber(tab, tab_length - 1)
   